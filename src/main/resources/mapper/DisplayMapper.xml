<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.site.vs.videostation.mapper.DisplayMapper" >
    <select id="findDisplayDataByTypeId" parameterType="int" resultType="Display">

		select v_id,v_name,v_pic,v_actor,v_publishyear,v_publisharea from sea_data where tid = #{tid}  order by v_addtime desc
	</select>

	<select id="findDisplayDataRecent" resultType="Display">
		select v_id,v_name,v_pic,v_actor,v_publishyear,v_publisharea  from sea_data order by v_addtime desc limit 5
	</select>

	<select id="findDisplayDataByTypeIds" resultType="Display">
		select  v_id,v_name,v_pic,v_actor,v_publishyear,v_publisharea  from sea_data where  tid in
		<foreach collection="list" item="tid" separator="," open="(" close=")">
			#{tid}
		</foreach>
	</select>

</mapper>